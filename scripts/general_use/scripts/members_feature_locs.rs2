[oploc1,dairy_churn]
if (map_members = ^false) {
    ~mesbox(^mes_members_feature);
    return;
}
~displaymessage(^dm_default);

[oploc1,dairy_churn_metal]
if (map_members = ^false) {
    ~mesbox(^mes_members_feature);
    return;
}
~displaymessage(^dm_default);

[oploc2,loom]
if (map_members = ^false) {
    ~mesbox(^mes_members_feature);
    return;
}
~displaymessage(^dm_default);
